# 下方了：英雄养成快照事件设计

更新时间：2026-09-02

## 1. 设计背景

现有英雄相关流水已经覆盖：
- `role_obtain_log`：英雄获得
- `role_uplv_log`：英雄升级
- `role_upadv_log`：英雄升阶
- `role_upstar_log`：英雄升星
- `role_reborn_lv_log`：英雄等级重生
- `role_back_star_log`：英雄星级回退
- `role_equip_*`：英雄装备相关
- `role_art_*`：神器相关
- `camp_alter_role_log`：共鸣祭坛英雄

这些事件适合分析“发生了什么变化”，但不适合直接回答“某个时间点玩家的英雄最终养成状态是什么”。因此新增英雄养成快照事件，用于 D1/D3/D7、流失前、困难关前后、付费前后等横截面分析。

## 2. 事件定义

- 事件名：`role_snapshot_log`
- 事件显示名：英雄养成快照
- 上报端：服务端
- 事件标签：英雄
- 事件粒度：1条 = 1个玩家 × 1个快照批次 × 1个已拥有英雄

### 建议字段

| 属性名 | 属性显示名 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| snapshot_date | 快照日期 | date/varchar | 是 | 快照归属自然日，建议 `yyyy-MM-dd` |
| snapshot_type | 快照类型 | varchar | 是 | `daily` / `checkpoint`，日常批处理默认 daily |
| snapshot_batch_id | 快照批次ID | varchar | 是 | 同一玩家同一批快照共用，方便检查完整性与去重 |
| ins_id | 英雄实例ID | varchar | 是 | 沿用现有英雄事件字段 |
| role_id | 英雄ID | varchar | 是 | 沿用现有英雄事件字段 |
| role_lv | 英雄等级 | double | 是 | 当前最终等级 |
| role_star | 英雄星级 | double | 是 | 当前最终星级 |
| role_advance | 英雄阶级 | double | 是 | 当前最终升阶/品质进度 |
| role_quality | 英雄品质 | double/varchar | 否 | 如果品质已能由 role_advance 完整映射，可不重复上报 |
| role_power | 英雄战力 | double | 是 | 当前英雄自身战力，不是玩家总战力 |
| is_alter | 是否共鸣 | boolean/double | 建议 | 是否处于共鸣祭坛 |
| alter_pos | 共鸣位置 | double | 否 | is_alter=1 时记录 |
| equip_power | 装备总战力 | double | 建议 | 建议只上报装备聚合战力，不拆每件装备明细 |
| art_lv | 神器等级 | double | 建议 | 当前神器等级 |
| art_star | 神器星级 | double | 建议 | 当前神器星级 |
| art_power | 神器战力 | double | 否 | 若服务端已有拆分战力则建议上报 |
| ring_id | 当前戒指ID | varchar | 否 | 未佩戴为空 |
| is_main_lineup | 是否默认主阵容 | boolean/double | 建议 | 用于区分“养了但不用”与核心英雄 |
| lineup_pos | 默认阵容位置 | double | 否 | is_main_lineup=1 时记录 |

公共事件属性继续使用下方了现有公共属性，如 `#account_id`、`nb_open_id`、`region_id`、`create_role_time`、`server_open_time`、`total_payment`、`power`、`season` 等，无需在事件内重复设计。

## 3. 推荐上报方式

### 首选：每日服务端批量快照

每天固定时间（例如次日 00:05~04:00）对“上一自然日有真实登录/登出行为的玩家”生成一次快照：

1. 先根据 `in_out_log` 确定上一自然日活跃玩家；
2. 每个活跃玩家生成一个 `snapshot_batch_id`；
3. 遍历该玩家当前已拥有英雄；
4. 每个英雄上报一条 `role_snapshot_log`；
5. `snapshot_date` 记录上一自然日，而事件发生时间可以记录实际批处理时间。

优点：
- 每个玩家每天只有一个确定状态，不会因一天多次登录/登出产生重复快照；
- 能准确保留流失前最后一个活跃日的英雄状态；
- 分析 D1/D3/D7、付费分层、困难关和英雄培养结构都非常直接；
- 与现有英雄养成流水互补，而不是替代。

### 不推荐：每次英雄养成变化都发快照

已有 `role_uplv_log`、`role_upstar_log`、`role_upadv_log`、装备和神器流水，再在每次变化时发完整快照会造成大量重复事件，数据量和服务端压力都偏高。

### 次选：登出时快照

如果服务端暂时无法做每日批任务，可在登出时上报，但必须做“自然日去重/覆盖”。单纯每次登出都上报会导致重度玩家一天产生多批快照，查询口径复杂且浪费数据量。

建议最终仍升级为每日批量快照。

## 4. 数据量控制建议

- 只对当天存在 `in_out_log` 的活跃玩家上报，不扫全量历史角色；
- 只上报当前已拥有英雄；
- 装备不要按每个槽位展开到快照事件，优先上报 `equip_power` 等聚合状态；
- 神器同理，只保留等级、星级、战力等核心状态；
- 不建议把所有英雄塞进一个 JSON/数组字段，一英雄一事件更适合数数平台直接 GROUP BY、分位数和漏斗/分层分析；
- `snapshot_batch_id` 必须保留，方便检查某个玩家当天快照是否完整以及服务端重跑去重。

## 5. 主要分析用途

该事件可以直接支持：
- D1/D3/D7 各付费层英雄等级、星级、阶级、战力分布；
- 各英雄获取后 N 日养成深度；
- 困难关玩家与正常玩家的核心英雄养成差异；
- 100元以上不同付费层“具体少养了什么”；
- 流失玩家最后活跃日英雄养成状态；
- 活动英雄是否真正进入主阵容，以及培养深度；
- 同一英雄不同版本/服务器批次的成长强度对比。

## 6. 结论

推荐采用 `role_snapshot_log`，服务端每日对上一自然日活跃玩家做一次批量快照，一英雄一条。流水事件负责解释“怎么变化”，快照事件负责回答“最终变成什么样”。两套数据结合后，比单纯依赖养成流水回放更稳定、查询成本更低。