# 暴弹飞射：升星本体足够但紫色狗粮不足口径（2026-09-02）

## 数据源与固定口径
- 项目：暴弹飞射。
- 用户表：`ta.v_user_42`。
- 事件表：`ta.v_event_42`。
- 正式服：`domain='release'`。
- 查询日期参数：**按新增日期筛选**，即 `${PartDate:date1}` 只作用于用户的创角日期，不用于限制后续英雄获得/升星事件日期。
- 新增天数：注册当天记为 D1，公式为 `date_diff('day', 新增日期, 统计日期) + 1`。
- 统计方式：按新增天数输出 D1、D2、D3……；每个新增天数使用截至该日结束的**累计英雄库存状态**判断是否被狗粮卡住。
- 首日付费：注册当天 `pay_log.payment>0` 汇总；`payment` 单位为元，不除以 100。
- 首日付费分层：`a_free`、`b_(0,6]`、`c_(6,30]`、`d_(30,100]`、`e_(100,300]`、`f_(300,500]`、`g_(500,1000]`、`h_(1000,+)`。
- 英雄获得：`role_obtain_log`，核心字段 `ins_id / role_id / hero_name / init_star / init_quality / init_lv / op_type`。
- 英雄升星：`role_upstar_log`，核心字段 `ins_id / role_id / hero_name / ostar / nstar / cost_thing_list`。
- 线上字段已确认：`role_upstar_log.cost_thing_list` 的仓库类型是 `array(row(num double, name varchar, cid double))`，不是 JSON/varchar；SQL 必须直接 `UNNEST(cost_thing_list)`，并按 3 个字段别名展开，禁止 `cast(cost_thing_list as varchar)` 再 `json_parse`。

## 2026-09-02 上传 role 配置确认的紫色狗粮
按用户定义：**紫色品质英雄全部视为狗粮**，并按阵营计算同阵营狗粮库存。

- 火：`100004 骑手蕉`、`100005 六耳蕉`
- 水：`100009 高雅蕉`、`100010 卡皮蕉`
- 风：`100014 魔法蕉`、`100015 僵尸蕉`
- 光：`100038 奶牛蕉`、`100039 爱神蕉`
- 暗：`100043 光头蕉`、`100044 魔藤蕉`

低品质（蓝/绿）不算本体，也不算紫色狗粮：`100016~100021`、`100045~100048`。
其余 `100001~100048` 中的橙/橙+英雄作为升星“本体”对象。

## 升星需求表
截图确认的“当前星级 -> 目标星级”中，本专题只比较“需要本体数”和“需要紫色狗粮数”：

| 当前星级 | 目标星级 | 需要本体 | 需要紫色狗粮 |
|---:|---:|---:|---:|
| 6 | 7 | 0 | 3 |
| 7 | 8 | 1 | 3 |
| 8 | 9 | 1 | 3 |
| 9 | 10 | 2 | 1 |
| 10 | 11 | 2 | 5 |
| 11 | 12 | 3 | 1 |
| 12 | 14 | 4 | 5 |
| 14 | 15 | 5 | 3 |

5->6 因狗粮需求为 0，不属于“本体够但狗粮不足”专题。

截图“其他”列（如目标 10、12、15 的额外条件）**暂不参与本专题判定**。因此结果表示“本体条件满足且紫色狗粮不足”，不代表紫色狗粮一定是唯一缺口；目标 10/12/15 可能同时缺少“其他”材料。

## 每个新增天数的库存重建口径
1. 先按新增日期筛选 cohort；事件日期不跟随 `${PartDate:date1}` 截断。
2. 对每个玩家生成从 D1 到当前已完整结束日期的每日统计节点。
3. `role_obtain_log` 累计统计从新增日到该统计日为止，每个 `role_id` 的累计获得实例数。
4. `role_upstar_log.cost_thing_list` 直接按 `array(row(num,name,cid))` UNNEST，累计扣减从新增日到该统计日为止已经消耗的英雄实例。
5. 当前星级取从新增日到该统计日为止，同一玩家、同一 `role_id` 最后一次 `nstar`；没有升星记录则取 `init_star`。
6. 某本体可用数量 = `累计剩余同名英雄实例数 - 1`，减去 1 是保留当前主英雄实例。
7. 某阵营可用紫色狗粮 = 截至该统计日，该阵营全部紫色英雄“累计获得数量 - 累计升星消耗数量”的合计。
8. “本体足够但狗粮不足” = `可用本体数 >= 目标星级所需本体数` 且 `同阵营可用紫色狗粮数 < 目标星级所需紫色狗粮数`。
9. 目标 7 星需要本体数为 0，因此当前为 6 星且同阵营紫色狗粮 < 3 时，属于“本体非瓶颈、狗粮不足”。

## 输出
按 `新增天数 + 首日付费分层 + 阵营 + 当前星级/目标星级` 输出：
- 本体足够玩家数
- 本体足够英雄数（玩家-英雄去重）
- 本体足够但狗粮不足玩家数
- 本体足够但狗粮不足英雄数
- 狗粮不足玩家占比
- 狗粮不足英雄占比

对应 SQL：`sql/baodanfeshe_d1_starup_fodder_gap_by_first_day_pay_v42.sql`。